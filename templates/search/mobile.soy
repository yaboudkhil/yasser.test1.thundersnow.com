{namespace mobile.search}

/**
 * @param soyTemplateName
 * @param? query
 * @param isHttps
 * @param logo
 * @param baseUrl
 * @param resultCount
 * @param locations
 * @param currentPage
 * @param resultsPerPage
 * @param locale
 * @param isPrimaryLocale
 * @param customParam
 */
{template .default}
      {call desktop.search.default data="all" /}
{/template}
